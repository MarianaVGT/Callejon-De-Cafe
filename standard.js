function clicked() {
    var removeItem = document.getElementById("button");
    var value = document.createElementById("add").value;
    li.appendChild(textNode);
    list.appendChild(li)
    button.onclick = removeItem;
    v
}